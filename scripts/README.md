# scripts/

Convenience wrappers and sbatch templates that aren't part of the Python package.

Examples (planned):
- `run_pipeline.sbatch` — run a configured pipeline as a slurm job.
- `rebuild_esmc_sif.sh` — re-trigger the esmc.sif build from its spec.
- `download_weights.sbatch` — re-download HF weight caches.
